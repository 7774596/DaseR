repos:
- repo: git@github.com:astral-sh/ruff-pre-commit.git
  # Ruff version.
  rev: v0.15.10
  hooks:
    # Run the linter.
    - id: ruff-check
      args: [ --fix ]
    # Run the formatter.
    - id: ruff-format
